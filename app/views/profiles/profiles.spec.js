'use strict';

describe('myApp.profiles module', function () {
  beforeEach(module('myApp.profiles'));

  describe('profiles controller', function () {
    it('should ....', inject(function ($controller) {
      // spec body
      var profilesCtrl = $controller('ProfilesCtrl');
      expect(profilesCtrl).toBeDefined();
    }));
  });
});
